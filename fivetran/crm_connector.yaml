connector:
  name: "CLV_Analytics_CRM_connector"
  service: "azure_sql_db"
  config:
    host: "[HOST_CRM]"
    port: 1433
    database: "CRM"
    user: "FIVETRAN_USER"
    password: "{{PLACEHOLDER_SOURCE_DB_PWD}}"
    update_method: "NATIVE_UPDATE" # Log-based CDC
  destination:
    service: "snowflake"
    account: "[SNOWFLAKE_ACCOUNT_ID]"
    database: "BRONZE"
    schema: "BRONZE_CRM"
  sync_frequency: "15" # Minutes
  schemas:
    - name: "CRM"
      tables:
        - name: "Customers"
          enabled: true
        - name: "CustomerRegistrationSource"
          enabled: true
        - name: "INCIDENTS"
          enabled: true
        - name: "INTERACTIONS"
          enabled: true
        - name: "SURVEYS"
          enabled: true